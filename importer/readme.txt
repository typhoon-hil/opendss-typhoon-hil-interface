Utilizes by default the portable Python installed with Control Center ("typhoon-python").
If you would like to use your own Python version, edit the import.bat file and remove "typhoon-"

Run install_requirements if packages are missing.